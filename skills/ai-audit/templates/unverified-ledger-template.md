# 未検証面台帳:{システム名}

対応する報告書:{報告書番号}
この台帳の目的:「指摘なし」と「安全」を区別する。ここに載っている領域は**検証していない**のであり、安全とも危険とも言えない。外部専門機関に追加検証を依頼する場合、この台帳がそのまま範囲指定(発注仕様)になる。

| # | 未検証領域 | 理由 | 想定されるリスク | 推奨する追加検証 |
|---|---|---|---|---|
| 1 | {例:本番サーバーのOS・ネットワーク設定} | 監査対象外(コードのみが対象) | 設定不備による侵入・漏洩 | インフラ脆弱性診断(外部機関) |
| 2 | {例:ツール未導入により複製コード計測を省略} | jscpd未導入 | 保守性の劣化を見逃す | ツール導入後の再検査 |
| 3 | {例:負荷試験は{N}件の模擬データまで} | 実データ量が未確定 | 大量データ時の性能劣化 | 実データ量確定後の負荷試験 |

**共通の限界(全監査に適用)**:
- 本監査はAIによる監査であり、未知の攻撃手法・監査AI自身の盲点は検出できない
- 実装AIと監査AIの異系統性は理論的な対策であり、両者が同じ誤りをする可能性はゼロではない
- 監査時点の依存パッケージ・脆弱性データベースに基づく(将来公表される脆弱性は対象外)
