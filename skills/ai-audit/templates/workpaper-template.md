# 監査調書:{システム名}

対応する報告書:{報告書番号} / この調書の目的:第三者が本監査を理解・再実行できる全証跡を残す

## W1. 監査環境

- 実施日時:{開始}〜{終了}
- 対象コミットID:`{hash}` / 作業ツリー:クリーン確認済み({git status結果})
- 実行環境:{OS、ランタイム、主要ツールのバージョン一覧(`tool --version` の出力)}
- 監査AI:Codex CLI {version} / モデル {model} / 推論設定 {effort} / セッションID {id}

## W2. リスク格付けの根拠(領域0)

- 影響度:{判定}/根拠:{仕様書の記述・ユーザー回答}
- データ機密度:{判定}/根拠:{〃}
- 自律性:{判定}/根拠:{〃}
- 決定メニュー:{簡易/標準/厳格}

## W3. ツール層検査の記録(Phase 2)

検査ごとに記録:

### {検査名}(基準 {領域-番号})
- 実行コマンド:`{コマンド}`
- ツール・バージョン:{name} {version}(未導入で代替手段を使った場合はその旨と手法)
- 結果要約:{検出件数・内容}
- 証拠出力:**マスキング済み**。{要約または `evidence/` 内のファイル参照}
- 秘密情報処理:{検出位置・種類・SHA-256短縮指紋。秘密値そのものは記録しない}
- 判定:{合/否/除外(理由)}

## W4. Codex独立監査の記録(Phase 3)

- 独立性の担保:Codexに渡した入力は仕様書パス・コードパスのみ(依頼文全文を下記に記録)
- 依頼文全文:
```
{プロンプト全文}
```
- Codex出力(マスキング済み)全文:
```
{出力全文}
```

## W5. 修正ループの記録(Phase 4)

| 周回 | 指摘(重大度) | 修正内容(コミット) | 再検証結果 |
|---|---|---|---|
| 1 | {内容} | {diff要約・コミットID} | {Codex出力要約} |

## W6. 能動的試験の記録(Phase 5)

- 逆向き検証:混入した欠陥{内容}/検出{可否}/欠陥除去確認:`git diff`結果{クリーン}
- おとり情報試験:投入した架空データ{概要}/出現確認箇所:{ログ・通信・出力の確認結果}
- 不変条件攻撃:条件{一覧}/シナリオ{一覧}/結果{一覧}
- 受入テスト(UAT):シナリオ{一覧}/証跡:`evidence/{ファイル名}`

## W7. 封印記録(Phase 6)

`seal.json` の内容と生成コマンド(`shasum -a 256 {対象}`)

## W8. 判定の例外・除外の記録

除外・重大度変更を行った項目とその理由(なければ「なし」)
